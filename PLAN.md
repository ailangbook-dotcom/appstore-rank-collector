# PLAN — 넘버드 (working title)

> 한 줄: **누르는 맛으로 붙잡고, 6시간마다 한 번 광고를 보면 스킨 하나가 나온다.**
> 그 스킨이 귀한지 아닌지는 **앱이 말해주지 않는다.** 사람들이 알아낸다.

## 0. 이 문서의 전제

- 확신도 **하**. 근거와 반례는 `viral/log.md` 2026-09-20 「사용자 제안 검증」 절에 전부 있다.
- 검증된 후보가 아니라 `캡팝`(리뷰 29)과 Steam `Banana`(91만 → 29만)를 역산한 **교정안**이다.
- **소재(클리커)는 유행이 아니다.** 2026-09-21 기준 실측:
  클리커 9개월 평탄 · 왁뿌볼 피크 대비 70%↓(3개월째 하강, 채널규모 중앙값 11.4만) ·
  볼꾸 1,186만 → 5.2만뷰 · 콜렉트북 월 1,430뷰 · 라부부 한국 미점화.
  **지금 올라탈 유행이 없다고 판단하고, 유행 편승을 포기한 설계다.**
  정체성은 트렌드가 아니라 **§3의 종(種) 16개**가 만든다.
- §8의 **중단 기준**을 먼저 읽어라. 지킬 생각이 없으면 시작하지 마라.

## 1. 만드는 것 / 안 만드는 것

### 만든다 (v1)

| | 내용 |
|---|---|
| 클리커 | 탭하면 소리 + 햅틱. **점수도 재화도 없다.** 순수 타격감. |
| 발급 | **6시간마다 1회.** 광고를 봐야 수령. 최대 2회까지 적립. |
| 스킨 | §3의 9축 조합으로 생성. 종 16개 안에서 변주된다. |
| 도감 | 내가 가진 스킨. 종별로 묶인다. |
| 인구 통계 | **축별 보유자 수.** "나선 무늬 — 356명" 같은 사실만. |
| 최근 발급 피드 | 방금 남들이 받은 스킨이 흐른다. 읽기 전용. |
| 공유 | 기울임이 담긴 **짧은 영상**. §4 참조. |

### 안 만든다

- **거래소** — 캡팝 리뷰 19건 중 5건(26%)이 거래소 장애다. ★4.21의 실체가 그것이다.
  소유권 이전 로직이 아예 없어야 이 앱이 안 무너진다. **v3 이전에 손대지 마라.**
- **랭킹 · 길드 · 환생 · 자동생산 · 미니게임** — 캡팝이 전부 갖고도 리뷰 29다.
  **기능으로는 캡팝을 못 이긴다.**
- **등급 · 확률 표시 · 연출** — §2 참조. 이게 이 앱의 정체성이다.
- **전역 발급 상한 / 마감** — 초안에 있었으나 **뺀다.** §2 참조.
- 배경 — 직접 구매(확률 없음)로 **나중에** 판다. v1 범위 밖.

## 2. 제1원칙 — 앱은 세기만 하고, 정의는 사용자가 한다

파충류 모프가 이 설계의 모델이다. 볼파이썬 알비노가 비싼 건 **열성 유전자 두 개가 겹쳐야**
나오기 때문이고, 그 확률은 유전학이 정한 것이다. 브리더가 한 건 확률을 만든 게 아니라 **값을 붙인 것**이다.

```
자연(유전자) = 확률을 정했다.  라벨은 안 붙였다.
사람(시장)   = 가치를 정했다.
```

그래서 **확률은 남기고 라벨만 버린다.**

### 화면에 절대 쓰지 않는 것

- `COMMON / RARE / EPIC / LEGENDARY` 같은 등급 이름
- `출현 확률 0.1%` 같은 확률 수치
- 등급별 테두리 색, 반짝이 이펙트, "축하합니다!" 연출
- **"전 세계 당신뿐입니다"** — 조합이 9.9억이라 전원에게 해당된다. 정보가 아니다.
- 전역 상한과 "마감" 알림 — **상한 자체가 "이건 귀한 겁니다"라는 선언**이다.
  유전자에는 상한이 없다. 확률이 낮을 뿐이다. 그래서 뺐다.

### 화면에 쓰는 것 (전부 사실이다)

- **발급 순번** — "12,043번째로 발급된 스킨"
- **축별 인구 통계** — "나선 무늬를 가진 사람 356명 / 홀로그램 몸통 554명"
- 발급 시각

인구조사지 가격표가 아니다. 어느 게 귀한지는 사람들이 이 숫자를 보고 **스스로 정한다.**

### 이 선택의 대가 (정직하게)

등급 라벨은 **즉시** 도파민을 준다. 창발적 가치는 **모수와 시간**이 필요하다 —
모프 시장은 수십 년 걸렸다. 사용자 1,000명 단계에선 아무 합의도 안 생기고
그냥 그림 하나 받고 끝날 수 있다. **이게 이 설계의 실질 리스크다.**
그걸 알면서도 이쪽으로 가는 이유는, 라벨 붙은 버전은 캡팝이 이미 하고 있고 리뷰 29이기 때문이다.

## 3. 스킨 구조 — 종(種) 고정 + 변주

### 왜 종이 필요한가: 합의는 이름 위에서만 생긴다

```
포켓몬  → 피카츄, 파이리          이름이 있다
모프    → 알비노, 파이볼드, 클라운   이름이 있다
왁뿌볼  → 슬랑이, 키캡, 탱글        이름이 있다
9축 조합 → "상단색 7 + 나선 3개"     ← 이건 대화가 아니다
```

"그거 있어?"가 성립해야 자랑도 별명도 생긴다. 그래서 **대화 단위를 16개로 묶는다.**

모델은 포켓몬이 아니라 **볼파이썬**이다:
포켓몬은 1,000종 × 변주 없음(개인이 못 만든다), 볼파이썬은 1종 × 모프 수백(만들 수 있다).

### 축 구성 (9축)

| 축 | 값 수 | 비고 |
|---|---|---|
| **종** | 16 | 고유 실루엣 + 이름. 직접 디자인한다. **대화 단위.** |
| 몸통 상단색 | 16 | hue |
| 몸통 하단색 | 16 | hue |
| **몸통 재질** | 6 | 상·하단이 **공유한다.** 실제 사물도 몸통 재질은 하나다. |
| 무늬 종류 | 12 | 점 · 줄무늬 · 별 · 하트 · 체크 · 물결 · 번개 · 꽃 · 해골 · 왕관 · 눈 · 나선 |
| 무늬 색 | 16 | hue |
| **무늬 재질** | 6 | 몸통과 따로 뽑는다 |
| 무늬 크기 | 5 | |
| 무늬 개수 | 7 | 1~7 |

**총 16 × 16 × 16 × 6 × 12 × 16 × 6 × 5 × 7 = 990,904,320 (약 9.9억)**

조합이 9.9억이라 **전원이 유일하다.** 그래서 유일함은 아예 언급하지 않는다 —
희소성 신호는 **축별 통계**다. 모수가 커서 의미 있는 숫자가 나온다.

### 재질 축 — 금색은 색이 아니다

`#FFD700`은 이름만 "gold"고 그냥 노란색이다. 금이 금으로 보이는 건 **명도 밴딩**(어두움 → 밝음 →
어두움) 때문이다. 그래서 금속성은 **색 축이 아니라 재질 축**에 있어야 한다.

| 재질 | 구현 | 가중치 |
|---|---|---|
| 무광 | 단색 | 34 |
| 유광 | 단색 + 상단 하이라이트 | 30 |
| 펄 | 부드러운 다중 밴딩 | 20 |
| 메탈릭 | 강한 명도 밴딩 + **기울기 반응** | 11 |
| 홀로그램 | `AngularGradient` 무지개 + **기울기 반응** | 3 |
| 야광 | 어두운 배경에서 발광 | 2 |

색 × 재질이라서 **금·은·동을 따로 박지 않는다**: 금 = hue 45° × 메탈릭, 은 = 210° × 메탈릭,
동 = 22° × 메탈릭. 그리고 **보라 메탈릭**처럼 실물에 없는 조합이 자연히 생긴다.

재질은 **라벨 없이 위계가 읽히는 유일한 축**이다. 색은 취향이라 서열이 안 생기지만,
무광보다 홀로그램이 귀해 보인다는 건 아무도 말해주지 않아도 안다.
포켓몬 카드가 쓰는 구조다 — 회사가 "홀로가 더 좋습니다"라고 한 적 없는데 시장이 안다.

**가중치는 코드에만 있다. 화면에 절대 안 띄운다.**

### 누적 20,000명 시점에 통계 화면에 뜰 숫자 (시뮬레이션)

```
몸통 재질   무광 6,776 · 유광 5,984 · 펄 4,086 · 메탈릭 2,200 · 홀로그램 554 · 야광 400
무늬 종류   점 2,727 · 줄무늬 2,654 … 왕관 999 · 눈 589 · 나선 356
무늬 개수   1개 6,734 · 2개 4,803 · 3개 3,139 … 6개 981 · 7개 419
```

무늬 개수는 **세면 되니까** 설명이 필요 없다. 7개를 받으면 보자마자 안다.

## 4. 기울기 — 목격형 장치

메탈릭과 홀로그램은 `CMMotionManager`의 `attitude.roll`을 그라디언트 각도에 물린다.
**폰을 기울이면 무늬가 흐른다.**

1. **정지 이미지로는 절반도 안 담긴다** → "직접 봐봐" 하고 **폰을 건네게 된다.**
   로그 기준 목격형은 가장 센 장치다(목탁·왁뿌볼).
2. **포카 홀로그램을 기울여 보는 행위가 이미 있다.** 새 행위를 만드는 게 아니다.
3. 대신 **공유는 짧은 영상**이어야 한다. 기울이는 동작이 찍혀야 의미가 있다.
   W3에서 `ImageRenderer` 한 장이 아니라 프레임 캡처 + 영상 인코딩이 된다. **공수가 는다.**

## 5. 기술 스택

| 항목 | 결정 | 이유 |
|---|---|---|
| 플랫폼 | iOS 네이티브 / SwiftUI, iOS 17+ | 타격감이 제품의 절반이다. 안드로이드는 v2. |
| 스킨 렌더 | SwiftUI `Canvas` + 축 값으로 절차적 생성 | 이미지 에셋은 **종 16개 실루엣뿐**. |
| 재질 | `LinearGradient`(메탈릭·펄) · `AngularGradient`(홀로그램) · 블러 레이어(야광) | |
| 기울기 | `CMMotionManager` → 그라디언트 `angle` | §4 |
| 사운드 | `AVAudioEngine` 다중 버퍼 | `AVAudioPlayer`는 연타에서 끊긴다. 캡팝 ★5가 "연타해도 삑사리 없다"를 칭찬한다. |
| 햅틱 | CoreHaptics (`CHHapticEngine`) | 소리와 **같은 프레임**에 터뜨린다. |
| 서버 | Supabase (Postgres) + RPC 1개 | 발급은 원자적이어야 한다. |
| 인증 | Supabase **익명 인증** | 사용자에겐 가입·로그인이 **안 보인다.** 서버에만 uid가 있다. |
| 광고 | AdMob **보상형만** | 전면·배너 금지. 캡팝 ★5가 "튀어나오는 광고 없어 좋다"를 칭찬한다. |
| 로컬 | SwiftData | 도감은 로컬이 원본. 서버는 발급 원장. |

### 서버 — 테이블 2개, 함수 1개

```sql
create table issues (
  id        bigserial primary key,        -- 이게 곧 전역 발급 순번
  owner     uuid not null,                -- 익명 uid
  species   smallint not null,
  body_top  smallint not null,
  body_bot  smallint not null,
  body_mat  smallint not null,
  ptn_kind  smallint not null,
  ptn_color smallint not null,
  ptn_mat   smallint not null,
  ptn_size  smallint not null,
  ptn_count smallint not null,
  issued_at timestamptz not null default now()
);

create table owners (
  uuid       uuid primary key,
  created_at timestamptz not null default now(),  -- +6시간의 기준
  last_issue timestamptz,
  banked     smallint not null default 0          -- 적립분 (최대 2)
);
```

```
RPC: issue_skin(p_owner uuid) -> 스킨 1행
  1. owners에서 created_at / last_issue / banked 확인
  2. 발급 가능 횟수 = floor((now - 기준)/6h) 에서 이미 쓴 만큼 뺀 값, 상한 2
  3. 0이면 에러 (남은 시간 반환)
  4. 각 축을 서버의 가중치표로 뽑는다  ← 확률표는 서버에만. 클라 조작 방지
  5. insert 후 id(=발급 순번)와 함께 반환
```

**타이머 기준은 로컬 시각이 아니라 `owners.created_at`이다.** 로컬이면 삭제 후 재설치로 무한 파밍된다.

### 받아들이는 한계

- **기기를 바꾸면 컬렉션이 사라진다.** 익명 uid의 대가다. v1에서는 받아들인다.
- 광고 시청 완료 → 발급 요청이 실패하면 **발급권을 로컬에 적립하고 다음 실행 때 재시도**한다.
  캡팝 ★1 "거래금 안 들어옴"이 이 처리를 안 해서 난 일이다.
- 발급권 조작 가능성은 v1에서 받아들인다. 어뷰징 방어에 시간 쓰면 3주가 깨진다.

## 6. 4주 일정 (평일 2~3시간)

> 초안은 3주였으나 **재질 축과 영상 공유 때문에 1주 늘었다.**

### W1 — 타격감만. 서버도 광고도 스킨도 없다.
- [ ] Xcode 프로젝트, SwiftUI 단일 화면
- [ ] 탭 → 눌림 애니메이션
- [ ] `AVAudioEngine` 다중 버퍼. **20회/초 연타에서 안 끊기는지 실기 확인**
- [ ] CoreHaptics 패턴을 소리와 동일 프레임에 정렬
- **종료 기준: 본인이 아무 이유 없이 3분 동안 탭하고 있으면 통과.** 아니면 여기서 멈춘다.

### W2 — 스킨 렌더러
- [ ] 종 16개 실루엣 디자인 (여기가 제일 오래 걸린다. 먼저 6개만 하고 나머지는 W4)
- [ ] 재질 6종 셰이딩 (`material-swatches.html` 참조)
- [ ] 무늬 12종 배치 (개수 1~7, 크기 5단계)
- [ ] `CMMotionManager` 기울기 → 그라디언트 각도

### W3 — 발급 파이프라인
- [ ] Supabase 테이블 2개 + RPC, 익명 인증
- [ ] 6시간 타이머 + 적립(최대 2) + 남은 시간 표시
- [ ] AdMob 보상형 (테스트 ID로 먼저) + 실패 시 발급권 적립·재시도
- [ ] 도감 (종별 묶기), 축별 인구 통계 화면, 최근 발급 피드

### W4 — 공유와 출고
- [ ] 기울임 영상 캡처 (프레임 캡처 → `AVAssetWriter`) + 공유 시트
- [ ] 종 나머지 10개
- [ ] App Store 등록물: 아이콘, 스크린샷, description, 개인정보 처리방침
- [ ] ATT 프롬프트, AdMob 운영 ID, `app-ads.txt`

## 7. 수익과 타겟 — 기대치를 먼저 낮춰라

**타겟은 초등학생이다.** 시간이 많고 수집 욕구가 있다. 왁뿌볼(리뷰 2,023)이 증명했다.

그런데 **광고 수익 모델과 구조적으로 충돌한다:**
아동 대상으로 처리하면 AdMob의 개인 맞춤 광고가 꺼지고 단가가 떨어진다.
Apple Kids Category(5.1.4)는 서드파티 광고·분석 제약이 더 세다. 결제력도 없다.

왁뿌볼처럼 "전 연령"으로 포지셔닝해 Kids Category를 피할 수는 있으나,
**광고 단가가 낮은 건 그대로다.**

> **결론: 수익을 기대하지 않는다.** 6시간 타이머는 수익 장치가 아니라 **발급 속도 제한 장치**로 본다.
> 성공은 매출이 아니라 확산으로만 판정한다(§8).

### 출시 전 확인할 것 (미확인 — 내가 검증하지 않았다)

- [ ] **한국 게임물 등급분류.** App Store 배포 시 Apple이 자체등급분류사업자라
      직접 GRAC 등급분류를 안 받아도 되는 것으로 알고 있으나 **확인 안 했다.**
- [ ] **확률형 아이템 확률 공시 의무**(게임산업법 2024-03 시행). 중소기업 예외가 있는 것으로
      알고 있으나 **확인 안 했다.** ⚠️ 이게 이 앱의 **가장 큰 법적 리스크**다 —
      §2에서 확률을 일부러 안 보여주기로 했는데, 공시 의무가 걸리면 설계 전제가 깨진다.
      **W1 안에 반드시 확인할 것.** 의무가 있다면 앱 안이 아니라 **외부 링크·웹페이지 표시**로
      가능한지까지 확인한다(그래야 화면은 깨끗하게 유지된다).
- [ ] AdMob 계정·세금·지급 설정은 리드타임이 있다. **W1에 미리 신청.**

## 8. 중단 기준 (먼저 정하고 시작한다)

| 시점 | 조건 | 행동 |
|---|---|---|
| W1 종료 | 타격감이 스스로를 3분 못 붙잡는다 | **중단.** 나머지 3주는 이 3분 위에 쌓는 것이다. |
| W1 중 | 확률 공시 의무가 있고 앱 내 표시가 강제된다 | 설계 재검토. §2 전제가 깨진다. |
| 출시 +2주 | 리뷰 < 5 **그리고** 게임/캐주얼 200위 밖 | 기능 추가로 버티지 마라. 캡팝이 그 길로 29다. |
| 출시 +4주 | 통계 화면에서 **홀로그램·나선 보유자가 두 자리도 안 된다** | 발급 모수가 없다는 뜻. **중단.** |
| 언제든 | 거래소를 만들고 싶어진다 | 캡팝 리뷰 5건을 다시 읽어라. 그 다음에도 만들고 싶으면 만들어라. |

**성공 신호는 리뷰 수가 아니라 이것 하나다: 기울임 영상이 앱 밖에서 관측되는가.**
`viral/discover.py probe <앱이름>` 과 네이버 검색으로 출시 후 매주 확인한다.
석가머니는 평시 1건 → 7건(7배)에서 차트 1위였다. 그 모양이 나오는지만 본다.

## 9. 이름

`넘버드` — iTunes Search **0건**으로 완전히 비어 있다(2026-09-20 확인).
대안: `몇번째`(1건, 무관) · `일련번호`(12건 전부 보증서·재고 관리 도구라 섞인다).

## 10. 기록

- 근거 전문: `viral/log.md` → 2026-09-20 「사용자 제안 검증 — 클리커 + 랜덤 스킨 + 유저 거래소」
- 재질 스와치: 스크래치의 `material-swatches.html`
- 참조: `캡팝`(6791143065) · `왁뿌볼` · Steam `Banana` · 볼파이썬 모프 · 포켓몬 카드 홀로
