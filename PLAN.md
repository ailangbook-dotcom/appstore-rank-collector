# PLAN — 일련번호 클리커 (working title)

> 한 줄: **탭하는 맛으로 붙잡고, 광고 1회에 스킨 1개를 발급하되, 그 스킨에 전 세계 공용 일련번호를 박는다.**
> 공유되는 건 스킨의 그림이 아니라 **`UNIQUE · 전 세계 1개 · 당신만 갖고 있습니다`** 라는 한 줄이다.

## 0. 이 문서의 전제 (읽고 시작할 것)

- 확신도 **하**. 근거는 `viral/log.md` 2026-09-20 「사용자 제안 검증」 절에 전부 있다.
- 이 설계는 **검증된 후보가 아니라**, 이미 출시된 `캡팝`(리뷰 29)과 Steam `Banana`(91만 → 29만)를
  역산해서 만든 **교정안**이다. 틀릴 수 있다는 전제로 공수를 3주로 묶었다.
- 그래서 §7의 **중단 기준**을 먼저 읽고, 지킬 생각이 없으면 시작하지 마라.

## 1. 무엇을 만드는가 / 무엇을 안 만드는가

### 만든다 (v1)

| | 내용 |
|---|---|
| 클리커 | 화면의 오브젝트를 탭한다. 소리 + 햅틱 + 카운트. **이게 전부다.** |
| 발급 | 보상형 광고 1회 시청 → 스킨 1개 발급. 하루 상한 있음. |
| 일련번호 | 발급 즉시 서버가 **등급 + 일련번호**를 준다. `EPIC #37 / 100` |
| 도감 | 내가 가진 스킨 목록. 각각 일련번호와 발급 시각이 박혀 있다. |
| 공유 카드 | 스킨 1장 + 일련번호 한 줄. 이미지로 저장/공유. |
| 마감 현황 | **소진된 등급 목록.** "LEGENDARY는 2026-10-04에 마감되었습니다." |

### 안 만든다 (v1에서 명시적으로 잘라냄)

- **거래소** — 캡팝 리뷰 19건 중 5건(26%)이 거래소 장애다. ★4.21의 실체가 이것이다.
  서버 + 계정 + 소유권 원장 + 어뷰징 대응이 개인 개발자를 잡아먹는다. **v3 이전에 손대지 마라.**
- **랭킹** — 캡팝이 랭킹 버그로 ★3을 두 번 맞았다. 없으면 버그도 없다.
- 길드 · 환생 · 자동생산 · 미니게임 · 보스전 — 전부 캡팝이 이미 갖고 있고, 그걸로도 29다.
  **기능으로는 캡팝을 못 이긴다.** 이긴다면 일련번호 한 줄로 이기는 것이다.

## 2. 핵심 설계 — 모순을 푸는 지점

원래 아이디어에는 모순이 있었다: **"모두가 서로 다른 스킨"** 과 **"아주 특이한 스킨"** 은 양립하지 않는다.
전부 1개씩이면 아무것도 희귀하지 않다. 크립틱나나가 230만원인 건 생성 확률이 아니라
**`25개 한정`이라는 공표된 고정 공급량** 때문이다.

그래서 둘을 분리한다:

```
외형  = f(seed)          → 무한히 다르다. 모두가 다른 그림을 갖는다.
희소성 = 등급별 전역 발급 상한 → 공표되고, 소진되면 영구 마감된다.
```

### 등급표 (숫자는 초안, 출시 전 고정하고 이후 절대 바꾸지 않는다)

| 등급 | 전역 상한 | 확률 | 소진 시 |
|---|---|---|---|
| COMMON | 무제한 | 80% | — |
| RARE | 10,000 | 15% | 마감 |
| EPIC | 1,000 | 4% | 마감 |
| LEGENDARY | 100 | 0.9% | 마감 |
| **UNIQUE** | **10** | **0.1%** | **마감** |

- 상한은 **전 세계 누적**이다. 내 것 10개가 아니라 인류 전체가 10개다.
- 소진되면 **영구 마감.** 나중에 들어온 사람은 절대 못 얻는다 → 조기 진입 동기가 생긴다.
- COMMON을 무제한으로 두는 이유: 상한이 다 소진돼도 신규 유저에게 줄 게 있어야 한다.
- 상한이 실제로 소진되기 시작하면 그때 **시즌제**(시즌마다 새 풀)를 검토한다. v1에는 없다.

### 발급 결과 화면 (이 화면이 제품의 전부다)

```
        ┌─────────────────┐
        │                 │   ← seed로 렌더된 스킨
        │   [ 스킨 그림 ]  │
        │                 │
        └─────────────────┘

             EPIC
      전 세계 1,000개 중 37번째
        2026-10-04 21:13 발급

        [ 공유 ]  [ 도감 ]
```

- 숫자는 **하나**여야 한다. 대시보드 만들지 마라 (인생영수증·바른걸음이 그래서 죽었다).
- UNIQUE가 뜨면 화면 전체를 쓴다: **"전 세계 10개 중 1번째. 당신뿐입니다."**

## 3. 기술 스택 — 결정 사항

| 항목 | 결정 | 이유 |
|---|---|---|
| 플랫폼 | **iOS 네이티브 / SwiftUI** | 타격감이 제품의 절반이다. CoreHaptics + AVAudioEngine을 그대로 쓴다. 안드로이드는 v2. |
| 최소 버전 | iOS 17 | SwiftUI 최신 API를 그냥 쓴다. 구버전 대응에 시간 쓰지 마라. |
| 스킨 렌더 | **SwiftUI Canvas + seed 기반 절차적 생성** | 이미지 에셋 0개. 앱 용량과 제작 공수를 동시에 없앤다. |
| 사운드 | AVAudioEngine 다중 버퍼 | 연타 시 `AVAudioPlayer`는 끊긴다. 캡팝 ★5 리뷰가 "연타해도 삑사리 없다"를 칭찬한다 — 여기서 지면 안 된다. |
| 햅틱 | CoreHaptics (`CHHapticEngine`) | `UIImpactFeedbackGenerator`보다 날카롭게 만들 수 있다. |
| 서버 | **Supabase (Postgres) + RPC 1개** | 발급은 원자적이어야 한다. Postgres는 이걸 한 줄로 한다. 무료 티어로 충분. |
| 인증 | Supabase **익명 인증** | 로그인 화면 없음. 캡팝은 구글 로그인 때문에 "기기 간 진행상황 다름" ★5 불만을 받았다. |
| 광고 | Google AdMob 보상형 | 보상형만. **전면/배너 넣지 마라** — 캡팝 ★5 리뷰가 "튀어나오는 광고 없어 좋다"를 칭찬한다. |
| 로컬 저장 | SwiftData | 도감은 로컬이 원본. 서버는 발급 원장일 뿐. |

### 서버는 이것 하나뿐이다

```sql
-- 발급 원장. 등급별 일련번호를 원자적으로 부여한다.
create table issues (
  id          bigserial primary key,
  owner       uuid not null,              -- 익명 인증 uid
  tier        text not null,              -- COMMON | RARE | EPIC | LEGENDARY | UNIQUE
  serial      int  not null,              -- 해당 등급 내 일련번호 (1부터)
  seed        bigint not null,            -- 외형 결정용
  issued_at   timestamptz not null default now(),
  unique (tier, serial)                   -- ← 상한 초과와 중복을 DB가 막는다
);

create table tier_caps (
  tier text primary key,
  cap  int                                -- null = 무제한(COMMON)
);
```

```
RPC: issue_skin(p_owner uuid, p_roll float) -> (tier, serial, cap, seed)
  1. p_roll로 등급 결정 (확률표는 서버에 둔다 — 클라 조작 방지)
  2. 해당 등급의 현재 발급 수 < cap 인지 확인. 초과면 한 등급 아래로 강등
  3. serial = coalesce(max(serial), 0) + 1  (트랜잭션 내, unique 제약이 최종 방어선)
  4. seed = random bigint
  5. insert 후 반환
```

**이게 서버 전부다.** 테이블 2개, 함수 1개. 하루면 끝난다.
그리고 이 구조는 거래소가 없으므로 **소유권 이전 로직이 존재하지 않는다** — 캡팝이 무너진 지점을 원천 회피한다.

### 클라이언트가 서버를 못 믿는 경우

- 광고 시청 완료 → 발급 요청이 네트워크 실패하면 **발급권(ticket)을 로컬에 적립**하고 다음 실행 때 재시도한다.
  캡팝 ★1 "거래금 안 들어옴"이 정확히 이 처리를 안 해서 난 일이다.
- 발급권은 로컬에만 있고 서버 검증이 없으므로 조작 가능하다. **v1에서는 받아들인다** —
  광고 보상이라 손해가 없고, 어뷰징 방어에 시간 쓰는 순간 3주가 깨진다.

## 4. 3주 일정 (평일 2~3시간 기준)

### W1 — 타격감만 만든다. 서버도 광고도 없다.
- [ ] Xcode 프로젝트, SwiftUI 단일 화면
- [ ] 탭 → 눌림 애니메이션 (스프링, 60→120fps 확인)
- [ ] AVAudioEngine 다중 버퍼로 연타 사운드. **20회/초 연타에서 안 끊기는지 실기 테스트**
- [ ] CoreHaptics 패턴. 소리와 **같은 프레임**에 터지게 맞춘다
- [ ] 누적 탭 카운트 로컬 저장
- **W1 종료 기준: 본인이 아무 이유 없이 3분 동안 탭하고 있으면 통과.** 아니면 여기서 멈춘다.

### W2 — 발급 파이프라인
- [ ] Supabase 프로젝트, 위 스키마 2테이블 + RPC 1개, 익명 인증
- [ ] seed → 스킨 외형 렌더러 (SwiftUI Canvas). 등급별로 팔레트·복잡도를 다르게
- [ ] AdMob 보상형 연동 (테스트 광고 ID로 먼저)
- [ ] 발급 플로우: 광고 완료 → RPC → 결과 화면. **네트워크 실패 시 ticket 적립·재시도**
- [ ] 하루 발급 상한 (초안: 5회)

### W3 — 공유와 출고
- [ ] 결과 화면 최종 (§2의 레이아웃). 숫자 하나, 크게
- [ ] 공유 카드 이미지 생성 (`ImageRenderer`) → 저장/공유 시트
- [ ] 도감 (그리드, 일련번호·발급시각)
- [ ] 마감 현황 화면
- [ ] App Store 등록물: 아이콘, 스크린샷 5장, description, 개인정보 처리방침
- [ ] ATT 동의 프롬프트, AdMob 운영 ID 교체, `app-ads.txt`

## 5. 출시 전 반드시 확인할 것 (아직 미확인 — 내가 검증하지 않았다)

- [ ] **한국 게임물 등급분류.** App Store 배포 시 Apple이 자체등급분류사업자라 개발자가
      직접 GRAC 등급분류를 받지 않아도 되는 것으로 알고 있으나 **확인 안 했다.** 직접 확인할 것.
- [ ] **확률형 아이템 확률 공시 의무** (게임산업법, 2024-03 시행). 매출 규모에 따른
      중소기업 예외가 있는 것으로 알고 있으나 **확인 안 했다.**
      어차피 §2 등급표를 공개할 거라 실무 부담은 작지만, **표시 위치·형식 요건**을 확인할 것.
- [ ] Apple 3.1.1 — 유료 루트박스는 확률 공시 의무가 명시돼 있다. 본 앱은 **광고 보상이라 무료**지만,
      심사에서 어떻게 보는지 확인할 것.
- [ ] AdMob 계정·세금 정보·지급 설정은 리드타임이 있다. **W1에 미리 신청해 둘 것.**

## 6. 이름 후보

`일련번호` · `넘버드(Numbered)` · `몇번째` · `전세계N개` · `딸깍 컬렉션`
— 검색으로 발견될 앱이 아니라 **공유로 발견될 앱**이므로, 이름은 공유 카드에 박혔을 때
설명이 필요 없는 것으로 고른다.

**중복 확인 완료 (2026-09-20, iTunes Search / kr):**
- `넘버드` → **0건.** 완전히 비어 있다.
- `몇번째` → 1건(`첫 번째 일기`, 리뷰 0). 무관.
- `일련번호` → 12건 전부 **보증서·재고·IMEI 관리 도구**. 그 이름을 쓰는 앱은 없다.

셋 다 쓸 수 있다. 다만 `일련번호`는 검색 시 보증서 관리 앱들과 섞이므로 **`넘버드`를 권한다.**

## 7. 중단 기준 (먼저 정하고 시작한다)

이 프로젝트의 승산 근거는 얇다. 매몰비용을 막기 위해 **미리** 끊는 선을 정한다.

| 시점 | 조건 | 행동 |
|---|---|---|
| W1 종료 | 타격감이 스스로를 3분 붙잡지 못한다 | **중단.** 나머지 2주는 이 3분 위에 쌓는 것이다. |
| 출시 +2주 | 리뷰 < 5 **그리고** 게임/캐주얼 200위 밖 | 기능 추가로 버티지 마라. 캡팝이 40종·거래소·길드로도 29다. |
| 출시 +4주 | UNIQUE·LEGENDARY 상한이 **전혀 안 깎임** | 발급 모수 자체가 없다는 뜻. **중단.** |
| 언제든 | 거래소를 만들고 싶어진다 | 캡팝 리뷰 5건을 다시 읽어라. 그 다음에도 만들고 싶으면 만들어라. |

**성공 신호는 리뷰 수가 아니라 이것 하나다: 공유 카드가 앱 밖에서 관측되는가.**
`viral/discover.py probe <앱이름>` 과 네이버 검색으로 출시 후 매주 확인한다.
석가머니는 평시 1건 → 7건(7배)에서 차트 1위였다. 그 모양이 나오는지만 본다.

## 8. 기록

- 근거 전문: `viral/log.md` → 2026-09-20 「사용자 제안 검증 — 클리커 + 랜덤 스킨 + 유저 거래소」
- 관련 규칙: 수치형 ④/⑤⑥⑦/⑧, 3겹 루프(① 동작=재화 ② 랭킹 ③ 커스텀)
- 참조 사례: `캡팝`(6791143065) · `왁뿌볼` · Steam `Banana`
